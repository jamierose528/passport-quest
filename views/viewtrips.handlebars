{{#if trips.length}}
  <div class="card">
    <h3>View Trips:</h3>

    {{#each trips as |trip|}}
      <div class="trips-list">
        <a id="view-all-trips" href="/viewTrips/{{trip.id}}">{{trip.city}}</a>
      </div>
    {{/each}}
  </div>
{{/if}}